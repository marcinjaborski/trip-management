import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#E97777",
    },
    secondary: {
      main: "#FCDDB0",
    },
    background: {
      default: "#FFFAD7",
    },
  },
});
